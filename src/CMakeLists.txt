file(GLOB CLUSTER_LIBRARY_CXX_SOURCES ${CLUSTER_SOURCE_DIR}/src/*.cpp)
file(GLOB CLUSTER_LIBRARY_CUDA_SOURCES ${CLUSTER_SOURCE_DIR}/src/*.cu)

include_directories(${CUDATHRUST_INCLUDE_DIR} ${CUDA_INCLUDE_DIR} ${TBB_INCLUDE_DIR} ${CLUSTER_SOURCE_DIR}/src/)
cuda_add_library(cluster ${CLUSTER_LIBRARY_CXX_SOURCES} ${CLUSTER_LIBRARY_CUDA_SOURCES})

